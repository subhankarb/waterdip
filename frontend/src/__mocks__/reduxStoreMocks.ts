export const create = () => {
  // const store = {
  //   getState: jest.fn(() => ({
  //     ...initialState
  //   })),
  //   dispatch: jest.fn(),
  //   subscribe: jest.fn()
  // };
  // const next = jest.fn();

  // const invoke = (action: any) => thunk(store)(next)(action);

  // return { store, next, invoke };
};
